# AIRBOT Play 仿真复现手册

> 目的：把 2026-07 求之科技实训里那条**真机**链路，在**没有硬件**的条件下重新跑一遍，
> 并且跑到每个结论都有对照组、每个数字都有原始输出留痕的深度。
>
> 本机复现环境：Ubuntu 24.04.4 / ROS 2 Jazzy / Python 3.12.3 / RTX 5880 Ada 48G / 96 核 / 251G 内存
> 复现目录：`/home/tz/workspace/airbot/sim-rerun`
> 全部命令的原始输出都在 `logs/`，可复现的脚本在 `scripts/`，截图在 `artifacts/`

---

## 0. 先搞清楚你在复现什么

原实训的完整链路是：

```
电机 → SocketCAN(can0) → airbot-arm 控制服务 → gRPC(:50051) → ROS 2 / MoveIt 2 → VLA 策略
                                                              ↑                    ↑
                                                          Part A               Part B
```

**没有硬件时，哪些环节能留、哪些必须替换：**

| 环节 | 真机 | 本次仿真复现 | 能不能说"我做过" |
| --- | --- | --- | --- |
| 电机 / CAN / airbot-arm 服务 | SocketCAN + `sudo airbot-arm -i can0 -t airbot_play_g2` | ❌ 无法复现，只能静态读代码 | 只能说真机时期做过，别吹 |
| ros2_control 硬件接口 | `airbot_hardware_py` 真机插件 | ✅ `mock_components/GenericSystem` 假硬件 | 能，但要讲清是 mock |
| MoveIt 2 规划 + 执行 | 同一套 | ✅ **完全相同**，一行代码不用改 | **能，这块是真本事** |
| 轨迹下发接口 | `follow_joint_trajectory` Action | ✅ 完全相同 | 能 |
| 关节状态回传 | `airbot_play_node` 50 Hz | ✅ `joint_state_broadcaster` 100 Hz | 能，但频率不同要说明 |
| VLA 数据采集 | airdc + 多相机录 mcap | ✅ MuJoCo 仿真里脚本化采集 | 能 |
| VLA 训练 | LeRobot + SmolVLA | ✅ **完全相同** | **能，这块也是真本事** |
| VLA 推理 | 真机 gRPC 下发 | ✅ MuJoCo 闭环 | 能 |

> 一句话记住：**规划器、控制器、Action 接口、训练框架都是同一套；换掉的只有最底下那层"硬件插件"。**
> 这正是 ros2_control 的设计意图——上层不知道下面是真电机还是 mock。

---

### 本次复现的完整拓扑（一张图看懂两条链路怎么接）

```
                     ┌───────────────────────── Part A：MoveIt 2 ─────────────────────────┐
                     │                                                                    │
  目标位姿/关节角 ──▶ move_group ──▶ moveit_simple_controller_manager                      │
                     │  · OMPL RRTConnect 采样                    │                       │
                     │  · 碰撞检测 (SRDF disable_collisions)      │                       │
                     │  · 时间参数化 (TOTG)                       ▼                       │
                     │                          /airbot_play_g2_controller/                │
                     │                              follow_joint_trajectory  (Action)      │
                     │                                          │                          │
                     │                            JointTrajectoryController                │
                     │                                  （插值 → position 命令接口）        │
                     └──────────────────────────────────┬───────────────────────────────────┘
                                                        │
                       ┌────────────────────────────────┴────────────────────────────────┐
                       │            ros2_control 硬件抽象层（换的就是这一层）              │
                       ▼                                                                 ▼
        【仿真】mock_components/GenericSystem                    【真机】airbot_hardware_py
          · 写进去 = 读出来，无动力学                              · gRPC → airbot-arm(:50051)
          · joint_state_broadcaster 发 /joint_states @100Hz       · SocketCAN can0 → 电机 PVT
                                                                  · airbot_play_node 发 /joint_states @50Hz


                     ┌───────────────────────── Part B：VLA ──────────────────────────────┐
                     │                                                                    │
   相机图像 ─┐        │   ┌──────────────┐   observation.images.{wrist,env0} 256×256×3      │
             ├──────▶ │   │  SmolVLA     │   observation.state  (8)                        │
   关节/EEF ─┘        │   │  450M 参数   │   task: "pick up the block and ..."             │
   语言指令 ─────────▶ │   └──────┬───────┘                                                 │
                     │          │  flow-matching 去噪 10 步                                │
                     │          ▼                                                          │
                     │   action chunk (50 × 8)  ──── 30 Hz 播放 ────▶ 执行层                │
                     └────────────────────────────────────────────────────────────────────┘
                                                        │
                       ┌────────────────────────────────┴────────────────────────────────┐
                       ▼                                                                 ▼
        【仿真】aao PolicyEvaluator                              【真机】gRPC → airbot-arm
          · MuJoCo 物理 600 Hz，动作更新 30 Hz                     · 同一套 action 语义
          · 按 stage 判定成功/失败并给出失败原因


   数据从哪来：
     真机  遥操作(lead 臂) → airdc 多相机同步录制 → mcap → 转 LeRobot 格式
     仿真  aao 脚本化专家 ConfigDrivenDemoPolicy → 直接写 LeRobotDataset（省掉 mcap 中转）
```

**看这张图要抓住的三件事：**

1. **两条链路共用最底下的执行层。** MoveIt 出的是"时间参数化的关节轨迹"，
   VLA 出的是"一串动作"，但它们最终都落到同一个硬件接口上。
2. **`ros2_control` 那一层是唯一被替换的东西。** 上面的规划器、控制器、Action 名字、
   训练框架，仿真和真机一个字都不用改。这就是为什么仿真复现是有意义的。
3. **两条链路的"保证"完全不同。** MoveIt 保证无碰撞、满足限位；VLA 什么都不保证。
   所以工程上正确的做法是 **VLA 出目标 → MoveIt 校验并执行**，而不是让 VLA 直接下发关节角。
   本次复现里这两条还是分开的，把它们接起来是最值得做的下一步。

---

## Part A：MoveIt 2 仿真链路（已跑通）

### A.1 装 MoveIt 2

仓库自带 `scripts/install_ros2_jazzy_deps.bash`，但它**没装全**课程验收要用的 ros2_control 组件
（原 SOP 1.1.2 也专门提醒了这点）。实际执行的是：

```bash
sudo apt-get update
sudo apt-get install -y \
  ros-jazzy-moveit \
  ros-jazzy-joint-state-publisher-gui \
  ros-jazzy-joint-trajectory-controller \
  ros-jazzy-joint-state-broadcaster \
  ros-jazzy-ros2controlcli \
  ros-jazzy-controller-manager \
  python3-colcon-common-extensions python3-rosdep python3-vcstool
```
> 日志：`logs/01-apt-moveit.log`

**为什么要单独装这四个包**：

| 包 | 缺了会怎样 |
| --- | --- |
| `ros2controlcli` | `ros2 control` 不是有效子命令（SOP 4.5） |
| `joint_state_broadcaster` | `Loader for controller 'joint_state_broadcaster' not found`，`/joint_states` 没有发布者（SOP 4.6） |
| `joint_trajectory_controller` | 控制器加载不了，`Plan & Execute` 无处下发 |
| `controller_manager` | 没有 `/controller_manager`，`ros2 control list_controllers` 一直等 |

装完的确切版本（**这是"版本快照"资产，环境崩了靠它还原**）：

```
ros-jazzy-moveit                     2.12.4-1noble.20260617.161956
ros-jazzy-moveit-core                2.12.4-1noble.20260617.141537
ros-jazzy-moveit-ros-move-group      2.12.4-1noble.20260617.150300
ros-jazzy-moveit-planners-ompl       2.12.4-1noble.20260617.150302
ros-jazzy-controller-manager         4.45.2-1noble.20260615.164916
ros-jazzy-joint-trajectory-controller 4.40.1-1noble.20260615.171409
ros-jazzy-joint-state-broadcaster    4.40.1-1noble.20260615.171040
ros-jazzy-ros2controlcli             4.45.2-1noble.20260615.165650
ros-jazzy-rviz2                      14.1.22-1noble.20260615.174609
```
> 完整快照：`notes/版本快照.md`

### A.2 拉代码并编译

```bash
git clone -b feature/jazzy \
  https://github.com/DISCOVER-Robotics/AIRBOT-Play-Hardware-with-Moveit2.git
cd AIRBOT-Play-Hardware-with-Moveit2

source /opt/ros/jazzy/setup.bash          # zsh 用 setup.zsh！
rosdep install --from-paths src --ignore-src --rosdistro jazzy -r -y
colcon build --symlink-install
source install/setup.bash                 # zsh 用 setup.zsh！
```

**注意仓库本身就是工作空间**（根目录直接有 `src/`），不要再套一层 `ws/src/`。

本次实测 commit：`48319430e86e8b5e7126fe2525c9fecf9f8cd1dd`（`feature/jazzy` 分支，2025-11-25）

编译结果（`logs/02-colcon-build.log`）：4 个包，2.5 秒。

> **为什么 4 个 ROS 包只要 2.5 秒？**
> 因为其中 `airbot_play_msgs` 的 `CMakeLists.txt` 里 `rosidl_generate_interfaces(...)` **整段被注释掉了**——
> 它是个空壳包，根本没生成任何消息类型。`airbot_play_description` / `airbot_play_movit_config` 是纯资源包，
> 只有 `airbot_play` 有 Python 节点。所以全工程没有一行 C++ 需要真编译。
> 能说出这句，说明你真的读过代码，而不是只跑过命令。

### A.3 每个新终端的环境加载（三层）

```bash
. /home/tz/workspace/airbot/sim-rerun/scripts/00-shell.sh
```

它做的事：

```bash
. env.sh                              # 1) 本次复现的路径变量
source /opt/ros/jazzy/setup.zsh       # 2) 系统 ROS 2 Jazzy + MoveIt 2
source $WS/install/setup.zsh          # 3) 本工作空间的 airbot_* 包
```

> **zsh 的坑（原 SOP 4.2 / `moveit仿真遇到的问题.md` 都记了）**：
> zsh 里 `source setup.bash` 会报 `没有那个文件或目录: .../setup.sh`。
> 原因是 ROS 2 的 `setup.bash` 用 `$BASH_SOURCE` 推算自己的路径，zsh 里这个变量是空的，
> 于是它拿**当前工作目录**去拼 `setup.sh`，必然找不到。
> **对策：按 shell 选后缀**，我在 `00-shell.sh` 里做了自动判断：
> ```bash
> if [ -n "$ZSH_VERSION" ]; then _sfx=zsh; else _sfx=bash; fi
> source /opt/ros/jazzy/setup.$_sfx
> ```

### A.4 启动仿真

```bash
ros2 launch airbot_play_movit_config demo.launch.py
```
> 脚本封装：`./scripts/01-sim-demo.sh`，日志：`logs/04-sim-demo.log`

**这一条命令到底起了什么**（必须能画出来）：

```
demo.launch.py  (moveit_configs_utils.launches.generate_demo_launch)
├─ robot_state_publisher     ← 读 URDF，发布 TF 树
├─ ros2_control_node         ← controller_manager，加载硬件插件
│    └─ FakeSystem  = mock_components/GenericSystem   ★ 假硬件在这里
├─ spawner joint_state_broadcaster       → 发布 /joint_states
├─ spawner airbot_play_g2_controller     → JointTrajectoryController
├─ move_group                ← MoveIt 2 规划服务，提供 /move_action
└─ rviz2                     ← MotionPlanning 面板
```

关键日志（自己去 grep 确认，别只看画面）：

```
[ros2_control_node] Loaded hardware 'FakeSystem' from plugin 'mock_components/GenericSystem'
[ros2_control_node] Successful 'activate' of hardware 'FakeSystem'
[spawner] Configured and activated joint_state_broadcaster
[spawner] Configured and activated airbot_play_g2_controller
```

**两条可以无视的 ERROR**（知道它们无害本身就是经验）：
- `Action server: /recognize_objects not available` — RViz 的物体识别面板，没接感知就没有，不影响规划
- `No 3D sensor plugin(s) defined for octomap updates` — 没配深度相机，八叉树避障不启用

### A.5 仿真验收（对应 SOP 2.2 / 4.11）

```bash
ros2 control list_controllers
ros2 control list_hardware_components
ros2 topic info /joint_states --verbose
ros2 topic hz /joint_states
ros2 topic echo --once /joint_states
ros2 action list
```

本次实测（`logs/05-sim-verify.log`）：

```
airbot_play_g2_controller  joint_trajectory_controller/JointTrajectoryController  active
joint_state_broadcaster    joint_state_broadcaster/JointStateBroadcaster          active

Hardware Component 1
  name: FakeSystem   plugin name: mock_components/GenericSystem
  state: id=3 label=active     read/write rate: 100 Hz
  command interfaces: joint1..joint6 /position [available] [claimed]

/joint_states  Publisher count: 1   (Node name: joint_state_broadcaster)
ros2 topic hz /joint_states → average rate: 99.999   (std dev 0.00014s)

/move_action
/execute_trajectory
/airbot_play_g2_controller/follow_joint_trajectory
```

**要读懂的三个数字：**

1. **`[claimed]`** — 命令接口被某个控制器独占了。同一个关节的 position 接口只能被一个控制器 claim，
   这就是为什么不能同时激活两个位置控制器。真机上"抢不到控制权"的租约（Lease）冲突是同一类问题。
2. **100 Hz** — 来自 `config/ros2_controllers.yaml` 的 `controller_manager.update_rate: 100`。
   **真机是 50 Hz**（`src/airbot_play/config/airbot_params.yaml` 的 `publish_rate: 50.0`）。
   "说 50 Hz，这里怎么 100 Hz"：仿真是 controller_manager 的循环频率，
   真机是 `airbot_play_node` 从 SDK 轮询电机状态的频率，两个数字来源不同。
3. **`Publisher count: 1`** — 只能有一个。如果是 2，说明仿真和真机的 launch 同时开着，
   MoveIt 会收到互相打架的状态，执行必然出问题。SOP 里专门要求"先确认仿真进程全停再启真机"就是这个道理。

### A.6 核心实验：Plan vs Plan & Execute

原 SOP 里这一步是在 RViz 里点按钮。我改成脚本，**因为要留下可复现的数字，而不是"我看着它动了"**。

```bash
python3 scripts/02-plan-vs-execute.py
```
> 日志：`logs/06-plan-vs-execute.log`

脚本干的事：向 `/move_action`（`moveit_msgs/action/MoveGroup`）发目标，
**两次唯一的差别是 `planning_options.plan_only` 这一个布尔量**。

实测结果：

| | error_code | 轨迹点数 | 规划耗时 | /joint_states 变化 |
| --- | --- | --- | --- | --- |
| `plan_only=True`（Plan） | 1 (SUCCESS) | 14 | 5.6 ms | `[0,0,0,0,0,0]` **完全不变** |
| `plan_only=False`（Plan & Execute） | 1 (SUCCESS) | 14 | — | 六个关节全部变到目标 |

目标 `[0.5, -0.3, 0.4, 0.0, 0.2, 0.0]`，执行后到位误差：

```
[0.000765, 0.000895, 0.000243, 0.000372, 0.000108, 0.000727]  rad
```

**必须能讲清的三件事：**

1. **RViz 里两个按钮的动画一模一样**，因为 Plan 也会把规划出的轨迹拿去播放预览。
   **唯一可靠的判据是 `/joint_states.position` 有没有变。** 这是 SOP 4.8 的原话，我用数字验证了。
2. **执行链路**：`move_group` 规划完 → 通过 `moveit_simple_controller_manager` →
   调用 `/airbot_play_g2_controller/follow_joint_trajectory` 这个 Action →
   `JointTrajectoryController` 插值 → 写 `mock_components` 的 position 命令接口 →
   `joint_state_broadcaster` 读回 → 发 `/joint_states`。
   **真机上唯一不同的是最后两步**：`airbot_play_node` 把轨迹转成 PVT 指令下发电机。
3. **为什么误差是 1e-4 量级而不是 0？** mock 硬件是理想的（写进去就是读出来），
   残差来自 `JointTrajectoryController` 的插值和 goal tolerance，不是"仿真精度"。
   真机上这个数会大得多，来源是减速比间隙、编码器分辨率、PVT 跟踪误差——这才是 Sim2Real 的一部分。

### A.7 反面用例（验证失败路径）

```bash
python3 scripts/03-negative-tests.py
```
> 日志：`logs/11-negative-tests.log`

| 用例 | 目标 | error_code | 轨迹点 | 耗时 |
| --- | --- | --- | --- | --- |
| 超关节限位 `joint2=-3.5` | 越界 | **99999 (UNDEFINED)** | 0 | 0.083 s |
| 大幅折叠姿态 | 合法 | 1 (SUCCESS) | 31 | 0.020 s |
| 对照组正常大幅度 | 合法 | 1 (SUCCESS) | 17 | 0.010 s |

**超限位那条的完整诊断链**（从 `logs/04-sim-demo.log` 里 grep 出来的）：

```
[WARN ] kinematic_constraints: Joint joint2 is constrained to be below the minimum bounds.
                               Assuming minimum bounds instead.
[ERROR] RRTConnect.cpp:265 - Unable to sample any valid states for goal tree
[WARN ] ParallelPlan::solve(): Unable to find solution by any of the threads in 0.0719 seconds
[ERROR] model_based_planning_context: Unable to solve the planning problem
```

**这四行要能一句话解释：**
MoveIt 先把越界的目标**夹紧到限位值**（-3.5 → -2.9671），但我给的容差是 ±1e-3，
夹紧后的目标区间正好压在限位边界上，OMPL 采样器采不到任何合法状态 → goal tree 建不起来 → 规划失败。
**它不是"算不出来"，是"目标本身就不合法"**——所以只花了 83 ms 就放弃，而不是耗满 3 秒的 `allowed_planning_time`。

对照 URDF 的真实限位（`xacro config/airbot_play_description.urdf.xacro | grep '<limit'`）：

```
joint1  [-3.1416,  2.0944]
joint2  [-2.9671,  0.17453]     ← -3.5 越界
joint3  [-0.087266, 3.1416]
joint4  [-3.0107,  3.0107]
joint5  [-1.7628,  1.7628]
joint6  [-3.0107,  3.0107]
g2_joint / g2_left / g2_right  [0, 0.0366]  (prismatic，夹爪行程 36.6 mm)
```

> 注意 joint2 的范围是 **[-2.97, +0.17]**——几乎全在负方向，joint3 几乎全在正方向。
> 这是机械结构决定的（大臂只能往一个方向弯）。"这个臂的工作空间为什么是这样"，答案在这。

### A.7.5 真正验证碰撞检测在工作（补一个严谨的用例）

A.7 里那个"折叠姿态"其实**没触发碰撞**，规划成功了——因为 SRDF 里相邻连杆的碰撞对被
`disable_collisions` 关掉了，随便挑个姿态是碰运气。
严谨的做法是**往 PlanningScene 里主动塞一个障碍物**，用同一个目标做三次对照：

```bash
python3 scripts/03b-collision-test.py
```
> 日志：`logs/24-collision-test.log`

脚本往 `/planning_scene` 发一个 `CollisionObject`：base_link 系下 0.4³ m 的盒子，
中心 (0.30, 0, 0.30)，正好压住目标位置。

**实测（同一个目标 `[0.5,-0.3,0.4,0,0.2,0]`，三次对照）：**

| | error_code | 轨迹点 | 耗时 |
| --- | --- | --- | --- |
| 1. 无障碍物 | **1** (SUCCESS) | 14 | 0.014 s |
| 2. 加了障碍物盒子 | **99999** (失败) | **0** | 0.012 s |
| 3. 移除障碍物后 | **1** (SUCCESS) | 14 | 0.022 s |

move_group 的日志把碰撞对逐个列出来了：

```
[ERROR] PlanningRequestAdapter 'CheckStartStateCollision' failed, because
'6 contact(s) detected : big_box - eef_connect_base_link, big_box - g2_base_link,
 big_box - g2_left_link, big_box - g2_right_link, big_box - link5, big_box - link6, '.
Aborting planning pipeline.
```

**这个实验的设计要点：**

1. **有对照组**。同一个目标，加/去障碍物各跑一次，加上恢复验证——
   排除了"目标本身有问题"这个混淆因素。这是实验设计的基本功。
2. **失败发生在规划前，不是规划中**。报错来自 `CheckStartStateCollision` 这个
   **PlanningRequestAdapter**，也就是 MoveIt 在把问题交给 OMPL **之前**就拒绝了。
   所以只花 12 ms，和 A.7 那个越限位用例的 83 ms 是同一类"快速失败"。
   > MoveIt 2 的规划管线是 `RequestAdapters → Planner → ResponseAdapters`。
   > 知道报错来自哪一环，就知道该往哪查。这条比记住 error_code 有用得多。
3. **它证明碰撞检测真的连着 PlanningScene**，而不是"配了但没生效"。
   很多人配完 SRDF 从来没验证过，上真机才发现避障是摆设。

**顺带解释 A.7 那个用例为什么没碰上**：
`airbot_play_description.srdf` 里有 30 多条 `disable_collisions`，
把所有 `reason="Adjacent"`（相邻连杆）和 `reason="Never"`（结构上永不相碰）的对都关了。
这是 MoveIt Setup Assistant 自动生成的——**它采样了大量随机姿态，把从没碰过的对标成 Never**。
所以在自由空间里靠折叠关节很难制造自碰撞，必须引入外部障碍物。

### A.8 真机分支的静态复核（没有硬件也要做）

```bash
# 1) 真机 launch 起了什么
cat src/airbot_play_movit_config/launch/movit_to_airbot.launch.py
# 2) 真机节点用的哪个解释器  ← 那个著名的坑
head -1 install/airbot_play/lib/airbot_play/airbot_play_node
# 3) 真机默认参数
cat src/airbot_play/config/airbot_params.yaml
```
> 日志：`logs/10-realrobot-static-review.log`

**实测输出（原样复现了当时的坑）：**

```
#!/usr/bin/python3
```

**这是当时最棘手的工程坑**：

- 现象：RViz 里 `Plan` 正常，`Plan & Execute` 返回 `CONTROL_FAILED`
- 完整报错：
  ```
  Motion plan was computed successfully.
  Didn't receive robot state (joint angles) with recent timestamp within 1.000000 seconds.
  Failed to validate trajectory: couldn't receive full current joint state within 1s
  CONTROL_FAILED
  ```
- 排查过程（**分层，从上往下**）：
  1. 规划成功 → 规划器和 URDF/SRDF 没问题，问题在执行侧
  2. `ros2 node list | grep airbot` → **没有 `/airbot_play`**，节点根本没起来
  3. `ros2 topic info /joint_states --verbose` → `Publisher count: 0`
  4. `head -1 .../airbot_play_node` → `#!/usr/bin/python3`
  5. `/usr/bin/python3 -c "import airbot_hardware_py"` → ImportError
- **根本原因**：colcon 生成的节点脚本 shebang 写死了系统解释器 `/usr/bin/python3`，
  而 SDK（`airbot_hardware_py`）只装在 Conda 环境里。终端里 `conda activate` 了也没用——
  **launch 起的子进程是按 shebang 走的，不看你终端激活了什么。**
  节点 import 失败后静默退出，MoveIt 只知道"收不到关节状态"。
- **解决**：
  ```bash
  export PYTHONPATH="$CONDA_PREFIX/lib/python3.12/site-packages:$PYTHONPATH"
  # 用节点实际使用的解释器验证，而不是用 python3 验证
  /usr/bin/python3 -c "import rclpy, airbot_hardware_py; print('OK')"
  ```
- **一般结论**：
  > 验证依赖时，必须用**目标进程实际使用的那个解释器**去验证，
  > 不能用当前终端的 `python3`。ROS 2 的 launch、systemd、cron、Docker ENTRYPOINT 都有同一类问题。

**真机默认参数里两个要警惕的**：

```yaml
publish_rate: 50.0        # 关节状态回传频率
auto_go_zero: true        # ★ 节点一起来机械臂就可能自己回零，跟你点不点 Plan&Execute 无关
allowed_start_tolerance: 0.01   # 起始状态和当前状态差超过 0.01rad 就拒绝执行
pvt_timeout: 10.0         # PVT（Position-Velocity-Time）轨迹下发超时
```

> `auto_go_zero: true` 是真机安全管理的核心：**启动即可能运动**。
> 所以 SOP 要求"固定底座、清空工作空间、急停待命"必须在敲回车之前完成，而不是之后。

---

## Part B：VLA 仿真链路

真机那次的链路是 **airdc 采集(mcap) → LeRobot 训练 SmolVLA → 真机 gRPC 推理**。
`airdc` 是私有仓库，拿不到；但同一个团队（DISCOVER-Robotics）把 **MuJoCo 仿真那一层开源了**，
就是 `auto-atomic-operation`（原 `lerobot.md` 里 `infer-aao` / `aao_sim` 说的那个）。
所以仿真版链路是：

```
aao 脚本化专家(MuJoCo) → LeRobot 数据集 → 训练 SmolVLA → aao 里闭环评测成功率
      ↑ 替代 airdc 遥操作采集                              ↑ 替代真机推理
```

### B.1 装环境

```bash
# 1) MuJoCo 仿真框架
GIT_LFS_SKIP_SMUDGE=1 git clone https://github.com/DISCOVER-Robotics/auto-atomic-operation.git
cd auto-atomic-operation
git config lfs.concurrenttransfers 32          # 默认 8，太慢
git lfs pull --exclude="assets/videos"          # 网格资产 ~150MB，演示视频用不上

# 2) Python 环境（uv 比 conda 快一个数量级）
uv venv --python 3.12 venv-vla
export VIRTUAL_ENV=$PWD/venv-vla
export UV_DEFAULT_INDEX=https://pypi.tuna.tsinghua.edu.cn/simple
uv pip install -e ".[mujoco]"
uv pip install imageio-ffmpeg opencv-python-headless
uv pip install torch torchvision
uv pip install "lerobot[smolvla]"
```

**关于 Git LFS 的那个坑（很容易卡半天）**

如果 `git lfs pull` 没跑或没跑完，MuJoCo 会报一个**完全看不出原因**的错：

```
stl_decoder: number of faces should be between 1 and 200000 in STL file
'.../assets/meshes/robotiq/base.stl'; perhaps this is an ASCII file?
```

`.stl` 现在还是个几百字节的文本指针，不是网格。判断方法：

```bash
file assets/meshes/robotiq/base.stl
#   "ASCII text"  → 还是指针，没拉下来
#   "data"        → 二进制 STL，OK
head -c 60 assets/meshes/robotiq/base.stl   # 指针文件开头是 version https://git-lfs.github.com/...
```

**下载路线要实测再选**，别想当然：

```bash
curl -sL -o /dev/null -w "直连: %{speed_download} B/s\n" --noproxy '*' <url>
curl -sL -o /dev/null -w "代理: %{speed_download} B/s\n" <url>
```

本次实测：GitHub raw 直连 **200 KB/s**、走代理 **93 KB/s**，看起来直连更快；
但 **LFS 对象存在 S3 CDN 上，直连根本连不通**（进度卡死在 35 MB 不动）。
最终是"走代理 + `concurrenttransfers 32`"跑通的。
**教训：`raw.githubusercontent.com` 的速度不代表 LFS 的速度，两者是不同的后端。**

### B.1.5 装 torch 这一步为什么会卡半小时（本次真实经历，值得单独写）

按教程直接 `uv pip install torch` 装了 **40 多分钟没装完**，全程不报错，就是慢。
排查和解决的完整过程：

**第 1 步：先确认是慢还是死。** 看进程还在不在、缓存目录还在不在长：
```bash
s1=$(du -sm ~/.cache/uv|cut -f1); sleep 30; s2=$(du -sm ~/.cache/uv|cut -f1)
echo "$(( (s2-s1)*1024/30 )) KB/s"
```
更可靠的是直接读网卡计数（不受文件预分配影响）：
```bash
iface=$(ip route get 223.5.5.5 | grep -o 'dev [^ ]*' | awk '{print $2}')
r1=$(cat /sys/class/net/$iface/statistics/rx_bytes); sleep 30
r2=$(cat /sys/class/net/$iface/statistics/rx_bytes); echo "$(( (r2-r1)/1024/30 )) KB/s"
```

**第 2 步：镜像测速，别信"清华最快"。** 实测同一时刻：
```
mirrors.aliyun.com        => 5238 KB/s
mirrors.ustc.edu.cn       => 3396 KB/s
pypi.tuna.tsinghua.edu.cn =>  286 KB/s   ← 当时被限速
```

**第 3 步：发现真正的原因——CUDA 版本太新。**
torch 默认拉的是 `nvidia-*-cu13` 系列 wheel。这些包**国内镜像还没缓存**，
镜像只能实时回源到 PyPI，速度 300 KB/s。换成 `torch==2.7.1`（依赖 `nvidia-*-cu12`）后，
同一个镜像 **2.5 MB/s**。
> **教训：镜像的价值在于"已经缓存过"。追最新版本 = 等于没有镜像。**
> 显卡是 Ada（sm_89），cu124/cu126 完全够用，追 cu13 零收益。

**第 4 步：多线程 + 多镜像并行。**
```bash
# 解析出精确依赖清单
uv pip install --dry-run "torch==2.7.1" "torchvision==0.22.1" "lerobot[smolvla]" \
  | grep -E "^\s*\+" | sed 's/^\s*+ //' > pkglist.txt
# 解析 wheel 直链（scripts/geturls.py，注意过滤架构和 Python tag）
python3 scripts/geturls.py > wheel_urls.txt
# 同一个文件挂 5 个镜像 × 16 连接
aria2c -i wheel_urls_multi.txt -j 4 -x 16 -s 16 --user-agent="pip/24.0"
# 离线安装
uv pip install --no-index --find-links /tmp/wheels -r pkglist.txt
```

**⚠️ 两个坑：**

1. **不加 `--user-agent` 会被镜像全部 403。**
   aria2c 的默认 UA 在阿里云 PyPI 镜像的黑名单里，表现是**每个文件瞬间 403**，
   看着像"所有链接都失效了"。但同一个 URL 用 `curl` 手测是 200。
   > **诊断口诀：curl 能下、批量工具全 403 → 十有八九是 User-Agent。**

2. **解析索引时必须过滤 CPU 架构和 Python tag。**
   偷懒"取最后一个 .whl"会拿到 `riscv64` / `aarch64` / `i686` / `macosx` 的包。
   本次 66 个包里有 10 个选错了架构。过滤条件：
   - 文件名含 `x86_64`，或以 `-any.whl` 结尾
   - Python tag 是 `cp312` / `py3-none` / `cp3X-abi3`（X ≤ 12）

**结果**：单线程 300 KB/s → 多镜像多线程 **5 MB/s**，同样的包从 40 分钟降到 6 分钟。

> **"下载慢"不是一个可以摊手的问题，
> 它可以被测量（网卡计数）、被归因（镜像没缓存新版本 / UA 被拦）、被解决（降版本 + 多源并行）。**

### B.2 先跑通仿真本身

**⚠️ 前置：每个新终端都要先敲这三行**，否则会报 `aao_demo: command not found`
（`aao_demo` 只装在 venv 里，系统 python 没有 `auto_atom` 模块）：

```bash
cd $SIMRUN/ws/auto-atomic-operation                          # Hydra 在这里找 aao_configs/，MuJoCo 在这里找 assets/
export PATH=$SIMRUN/ws/venv-vla/bin:$PATH                    # 让 aao_demo / python 指向 venv
export MUJOCO_GL=egl                                         # 无显示器时的 GPU 离屏渲染
```

```bash
aao_demo --list                       # 67 个任务配置
aao_demo --config-name mock                                   # 纯 mock，不需要 MuJoCo 资产
aao_demo --config-name pick_and_place +env.viewer.disable=true # 真 MuJoCo
```

> **Hydra 的 `+` 是什么意思**：Hydra 默认开 struct 模式——
> 配置里**已有**的 key 用 `k=v` 覆盖，配置里**没有**的 key 必须用 `+k=v` 新增。
> 报 `Could not override 'X'. To append to your config use +X=...` 就是这个原因。

实测结果（`logs/14-aao-pick-and-place.log`）：

```
Success rate: 2/2
loop_frequency: 119.7 Hz (254 steps in 2.122s)
completed_stage_info: {'pick_source': ['succeeded','succeeded'],
                       'place_source': ['succeeded','succeeded']}
sim_time: min=8.267s, max=8.467s
```

**要读懂的几点：**
- **2/2 是因为 `env.batch_size: 2`** —— MuJoCo 同时跑 2 个并行环境副本，每个的物块初始位置不同
  （`task.randomization.source_block.x/y: [-0.03, 0.03]`）。这是"专家策略对随机初始位姿的鲁棒性"。
- **两个 stage**：`pick_source` → `place_source`。框架自带 stage 级的成功/失败判定和失败原因，
  所以后面评测策略时能拿到"卡在哪个 stage"，而不只是一个 0/1。
- **119.7 Hz 是控制循环频率，8.4 s 是仿真世界时间**。`sim_freq: 600`、`update_freq: 30`，
  即物理步进 600 Hz、动作更新 30 Hz——**30 Hz 就是后面数据集的 fps**。

### B.3 先看清"特征契约"，再动手做数据集

**这是最容易翻车的一步，也是原总结里点名的"最常见报错根源"。**
不要照抄别人的 yaml，先把观测字典打出来亲眼看：

```bash
# 注意 ../../ ：脚本在 sim-rerun/scripts/，但工作目录必须是 aao 仓库
python ../../scripts/04-dump-obs-keys.py --config-name pick_and_place +env.viewer.disable=true
```

实测（`logs/15-obs-keys.log`，21 个 key，节选）：

```
action/arm/pose/position              (2, 3)  float32   ← 动作
action/arm/pose/orientation           (2, 4)  float32   ← 动作
action/gripper/joint_state/position   (2, 1)  float64   ← 动作
arm/pose/position                     (2, 3)  float32   ← 状态
arm/pose/orientation                  (2, 4)  float32   ← 状态
gripper/joint_state/position          (2, 1)  float64   ← 状态
wrist_cam/color/image_raw             (2, 352, 640, 3) uint8   ← 腕部相机
env0_cam/color/image_raw              (2, 352, 640, 3) uint8   ← 环境相机
env1_cam/color/image_raw              (2, 352, 640, 3) uint8
（还有 depth / mask heat_map / 触觉点云，本次不用）
```

**和真机那次完全对得上**（Ds 对话记录里的原文）：

| | 真机 (airdc) | 仿真 (aao) |
| --- | --- | --- |
| 状态 | `/follow/eef/pose/position`(3) + `orientation`(4) + `joint_state/position`(1) = **8** | `arm/pose/position`(3) + `orientation`(4) + `gripper`(1) = **8** |
| 动作 | `/lead/...` 同样 8 维 | `action/...` 同样 8 维 |
| 图像 | `/env_camera/...` + `/follow_camera/...` | `env0_cam` + `wrist_cam`，`(352,640,3)` |

> 第一维的 `2` 是 batch（两个并行环境），不是特征维度。**这个必须分清**，
> 否则会把 batch 当成特征塞进 `observation.state`，然后在归一化时报一个莫名其妙的维度错。

**为什么姿态用四元数（4 维）而不是欧拉角（3 维）**：
欧拉角有万向锁和 ±π 的不连续，网络回归时在跳变处会学出巨大的梯度。
四元数连续但有 `q ≈ -q` 的双覆盖问题（同一个姿态两种表示），
所以更好的做法是用 **rotation_6d**（观测里也有 `arm/pose/rotation_6d`，(2,6)）——
它是旋转矩阵的前两列，连续且唯一，是目前 VLA/位姿回归的主流选择。
本次为了和真机那 8 维契约保持一致，仍用四元数。

### B.4 采集示教数据 → LeRobot 数据集

```bash
cd ws/auto-atomic-operation
export PATH=/home/tz/workspace/airbot/sim-rerun/ws/venv-vla/bin:$PATH
export MUJOCO_GL=egl
python 05-collect-lerobot-dataset.py --config-name pick_and_place \
  +env.viewer.disable=true \
  +collect.episodes=60 \
  +collect.out=/home/tz/workspace/airbot/sim-rerun/artifacts/ds_pick_place
```
> 日志：`logs/18-collect-dataset.log`

**本次实测产出**：

```
总集数 60 | 总帧数 14954 | 平均每集 249.2 帧 | fps 30 | 磁盘 69 MB
失败被丢弃的轨迹: 0 条（脚本化专家在 ±3cm 随机初始位姿下 60/60 全成功）
```

数据集目录结构（标准 LeRobot v3 布局）：

```
artifacts/ds_pick_place/
├── meta/
│   ├── info.json          ← features 定义、fps、总帧数
│   ├── stats.json         ← ★ 归一化统计量，训练/推理都靠它
│   ├── tasks.parquet      ← 语言指令表
│   └── episodes/chunk-000/
├── data/chunk-000/        ← state / action / index 的 parquet
└── videos/
    ├── observation.images.wrist/chunk-000/file-000.mp4
    └── observation.images.env0/chunk-000/file-000.mp4
```

`info.json` 里的 features（**这就是"特征契约"的书面形式**）：

```
observation.state          float32  [8]
action                     float32  [8]
observation.images.wrist   video    [256, 256, 3]
observation.images.env0    video    [256, 256, 3]
timestamp / frame_index / episode_index / index / task_index
```

**采集脚本做的事**（`scripts/05-collect-lerobot-dataset.py`）：

1. 每条轨迹换一个 `task.seed` → 物块 x/y 在 ±3 cm 内随机（对应真机每次摆放位置不同）
2. 用 aao 自带的**脚本化专家**（`ConfigDrivenDemoPolicy`）跑完整任务，逐帧抓观测
3. 拼成 8 维 state / 8 维 action + 两路 256×256 图像
4. **只保留成功的轨迹**——失败的示教数据会毒化策略
5. 直接调 `LeRobotDataset.create()` / `add_frame()` / `save_episode()`

**几个必须理解的设计决定：**

| 决定 | 为什么 |
| --- | --- |
| 图像下采样到 **256×256** | 原始 352×640 非方形且太大；VLA 视觉塔输入通常是方形。不下采样只是白占磁盘，最后还是会被 resize |
| **只保留成功轨迹** | 模仿学习在拟合示教分布。塞进失败轨迹等于教模型"这样做也行" |
| `fps=30` | 来自 `basis.yaml` 的 `update_freq: 30`。**必须和仿真的动作更新频率一致**，否则时间戳和真实动作节奏对不上 |
| **两路相机**（wrist + env0） | 单路环境相机看不清夹爪与物体的相对关系，单路腕部相机看不到全局。"全局 + 腕部"是 VLA 的标准配置 |
| 用**专家策略**而不是遥操作 | 仿真里没有主臂。好处是一致性高、可批量；坏处是**没有人类的犹豫和纠错行为**，模型学不到"错了怎么救回来"。这是仿真数据的固有局限 |

**两个 API 坑（lerobot 0.6.1）：**

1. `add_frame(frame, task=...)` 的 `task` 参数**已经取消**了，现在要放进 frame 字典：
   ```python
   fr["task"] = "pick up the block and place it on the pedestal"
   ds.add_frame(fr)          # 不是 ds.add_frame(fr, task=...)
   ```
2. **`torchcodec` 加载失败会回退 PyAV，这是预期行为**，不是错误：
   ```
   OSError: libavutil.so.57: cannot open shared object file
   'torchcodec' is installed but cannot be loaded. Falling back to 'pyav' as a default decoder.
   ```
   原 `lerobot.md` 里就写了"视频解码用 PyAV（torchcodec 有意不装）；日志里的 pyav 回退提示是预期行为"。
   要消掉它得装匹配版本的系统 FFmpeg 库，**没必要**。


```bash
cd ws/auto-atomic-operation
export MUJOCO_GL=egl
python ../../scripts/05-collect-lerobot-dataset.py --config-name pick_and_place \
  +env.viewer.disable=true \
  +collect.episodes=40 \
  +collect.out=/home/tz/workspace/airbot/sim-rerun/artifacts/ds_pick_place
```

脚本做的事（`scripts/05-collect-lerobot-dataset.py`）：

1. 每条轨迹换一个 `task.seed` → 物块的 x/y 在 ±3 cm 内随机（对应真机每次摆放位置不同）
2. 用 aao 自带的**脚本化专家策略**（`ConfigDrivenDemoPolicy`）跑完整任务，逐帧抓观测
3. 把观测拼成 8 维 state / 8 维 action + 两路 256×256 图像
4. **只保留成功的轨迹**——失败的示教数据会毒化策略
5. 直接调 `LeRobotDataset.create()` / `add_frame()` / `save_episode()` 写标准数据集

**几个必须理解的设计决定：**

| 决定 | 为什么 |
| --- | --- |
| 图像下采样到 **256×256** | 原始 352×640 非方形且太大；VLA 视觉塔的输入通常是方形（SigLIP 224/256）。不下采样会在预处理时被强行 resize，还白白占磁盘 |
| **只保留成功轨迹** | 模仿学习是在拟合示教分布。塞进失败轨迹等于教模型"这样做也行" |
| `fps=30` | 来自 `basis.yaml` 的 `update_freq: 30`。**必须和仿真的动作更新频率一致**，否则 LeRobot 算出来的时间戳和真实动作节奏对不上 |
| **两路相机**（wrist + env0） | 单路环境相机看不清夹爪和物体的相对关系，单路腕部相机看不到全局。VLA 的标准配置就是"全局 + 腕部" |
| 用**专家策略**而不是遥操作 | 仿真里没有主臂。好处是数据一致性高、可批量；坏处是**没有人类的犹豫和纠错行为**，模型学不到"错了怎么救回来"。这是仿真数据的固有局限 |

### B.4.7 SmolVLA 基座权重：两个仓库，不是一个

`lerobot/smolvla_base` 只是"动作专家"那一半，它的 `config.json` 里写着骨干是另一个仓库：

```
type: smolvla
vlm_model_name: HuggingFaceTB/SmolVLM2-500M-Video-Instruct   ← 视觉语言骨干，要单独下
chunk_size: 50          n_action_steps: 50    ← 一次推理输出 50 个动作（action chunking）
num_steps: 10                                 ← flow-matching 去噪 10 步
n_obs_steps: 1                                ← 只看当前这一帧，不看历史
max_state_dim: 32       max_action_dim: 32    ← 内部 pad 到 32，我们的 8 维会被补零
freeze_vision_encoder: True                   ← 默认冻结视觉塔
resize_imgs_with_padding: [512, 512]
```

**两个数字直接印证了真机那次的经验**：
- `chunk_size = n_action_steps = 50` —— 当年为了解决"每步 1.25 秒、机械臂一顿一顿"，
  改成一次推理返回 50 个动作、本地 30 Hz 播放。**这不是我们自己想出来的技巧，是 SmolVLA 的原生设计。**
- `num_steps: 10` —— 10 步去噪。推理成本几乎全在这里，所以"输出 1 个动作"和"输出 50 个动作"
  成本一样，chunking 才能把单动作成本摊到 1/50。

**`max_state_dim: 32` 要理解对**：SmolVLA 用一个固定宽度的输入层接不同机器人，
维度不够就补零。所以 8 维状态**不会报错**，但你必须保证训练和推理**补零的方式一致**——
这就是"特征契约"在模型内部的体现。

**国内下载这两个仓库的正确姿势**（两个坑）：

```bash
export HF_ENDPOINT=https://hf-mirror.com
export HF_HUB_DISABLE_XET=1        # ★ 坑 1
unset all_proxy ALL_PROXY          # ★ 坑 2
```

1. **`HF_HUB_DISABLE_XET=1`**：新版 `huggingface_hub` 默认走 Xet CAS 传输后端，
   而国内镜像不实现这个协议，它会绕过镜像直连 `cas-server.xethub.hf.co` 然后 **401 Unauthorized**。
   报错长这样，很容易误判成"要登录"：
   ```
   RuntimeError: Task error: File reconstruction error: CAS Client Error:
   Request error: HTTP status client error (401 Unauthorized),
   domain: https://cas-server.xethub.hf.co/v2/reconstructions/...
   ```
2. **`all_proxy=socks://...` 会让 httpx 直接崩**：
   ```
   ValueError: Unknown scheme for proxy URL URL('socks://127.0.0.1:7897/')
   ```
   httpx 只认 `socks5://`，不认 `socks://`。而很多代理客户端默认就写 `socks://`。
   **只留 `http_proxy` / `https_proxy`，把 `all_proxy` unset 掉。**

嫌 `snapshot_download` 慢的话，同样用 aria2c 直取（速度从 ~200 KB/s 到 2 MB/s）：
```bash
curl -s https://hf-mirror.com/api/models/lerobot/smolvla_base \
  | python3 -c "import sys,json;print('\n'.join(f['rfilename'] for f in json.load(sys.stdin)['siblings']))"
# 生成 aria2 输入文件后
aria2c -i urls.txt -j 4 -x 16 -s 16 --user-agent="pip/24.0"
```

### B.4.9 训练前必看：打开归一化统计量

数据采完**先别急着训**。`meta/stats.json` 里有 LeRobot 算好的 mean/std/min/max，
这是"亲眼确认特征契约"的最后一步，也是本次复现里发现的最有价值的一个问题。

```bash
python3 -c "
import json; s=json.load(open('artifacts/ds_pick_place/meta/stats.json'))
for k in ['observation.state','action']:
    print(k, {kk:[round(x,4) for x in v] for kk,v in s[k].items() if kk in ('mean','std')})
"
```

实测（`logs/21-dataset-stats.log`）：

```
--- observation.state ---   [x, y, z, qx, qy, qz, qw, gripper]
  mean: [0.0246, -0.0098, 0.2102, -0.0002, 0.7071, -0.0005, 0.7071, 0.2526]
  std : [0.0849,  0.0351, 0.0592,  0.0012, 0.0005,  0.0014, 0.0006, 0.2486]
--- action ---
  mean: [0.0344, -0.0024, 0.2033, 0.0, 0.7071, 0.0, 0.7071, 0.3919]
  std : [0.0814,  0.0149, 0.0677, 0.0, 0.0,    0.0, 0.0,    0.4078]
                                  ↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑
                                  四元数四维的 std 全是 0
```

**这说明什么：**

1. **8 维 action 里，只有 4 维（x, y, z, gripper）真正携带信息。**
   四元数四维恒等于 `[0, 0.7071, 0, 0.7071]`——因为脚本化专家全程保持同一个末端姿态（夹爪垂直向下）。
2. **std = 0 是归一化的雷。** `(x - mean) / std` 会除零。LeRobot 内部会 clamp，不会崩，
   但训练时这四维的梯度信号是纯噪声，白白占了模型容量。
3. **`observation.state` 的四元数 std ≈ 1e-3**，也是近似常量——那是物理仿真的微小抖动，不是任务信号。

**该怎么处理：**

| 做法 | 适用 |
| --- | --- |
| 什么都不改，照训 | 最省事，模型会自己学会"这四维永远输出定值"。本次采用，作为基线 |
| 砍掉退化维度，只训 4 维 | 更干净，但推理时要把常量姿态补回去，且换任务就得重来 |
| **让专家的姿态也随机化**（`randomization` 里加 `yaw`） | **最治本**：数据本身就该覆盖姿态变化，否则模型永远学不会转手腕 |

> 原 `pick_and_place.yaml` 里其实留了一行注释掉的 `# yaw: [-0.524, 0.524]   # ±30°`。
> 把它打开，四元数维度立刻就有方差了。**这一行注释就是"数据多样性不足"的物证。**

**这件事的准确表述：**
> "我训练前先看了数据集的归一化统计量，发现 8 维动作里有 4 维标准差是 0——
> 专家策略全程保持同一个末端姿态。这意味着模型在这四维上学不到任何东西，
> 而且换成需要转手腕的任务时会直接失效。根因是采集时没打开姿态随机化。"

这比"我训了个模型，成功率 XX%"有说服力得多——**它证明你看过数据，而不只是跑过脚本。**

### B.5 训练 SmolVLA

一条命令（封装在 `scripts/07-train-smolvla.sh`）：

```bash
export HF_HOME=$SIMRUN/ws/hf_cache
export HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1     # 权重已在本地，强制离线
export HF_HUB_DISABLE_XET=1
export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True

lerobot-train \
  --policy.type=smolvla \
  --policy.pretrained_path=$SIMRUN/ws/smolvla_base \        # 动作专家（本地目录）
  --policy.vlm_model_name=$SIMRUN/ws/smolvlm2_base \        # 视觉语言骨干（本地目录）
  --policy.device=cuda \
  --policy.push_to_hub=false \
  --dataset.repo_id=local/airbot_sim_pick_place \
  --dataset.root=$SIMRUN/artifacts/ds_pick_place \
  --batch_size=24 --steps=12000 \
  --save_freq=4000 --log_freq=100 \
  --output_dir=$SIMRUN/artifacts/train_smolvla \
  --wandb.enable=false
```

**为什么两个 `--policy.*` 路径都要显式给本地目录**：
`smolvla_base/config.json` 里的 `vlm_model_name` 写的是 HF repo id
（`HuggingFaceTB/SmolVLM2-500M-Video-Instruct`），离线时解析不到。
把它覆盖成本地目录，`transformers` 会按本地路径加载，全程不碰网络。

**训练时要盯什么（不是只看 loss 下降）：**

1. **`nvidia-smi` 的显存**。SmolVLA 本体不大，显存主要被 `batch × 图像分辨率` 吃掉。
   OOM 时按代价从小到大加：
   ```
   1. PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True   几乎零代价，减少碎片
   2. AMP 混合精度                                        几乎零代价
   3. gradient_checkpointing                             约 30% 算力代价
   4. batch_size 减小 + grad_accumulation_steps 补回      影响收敛
   5. freeze_vision_encoder（SmolVLA 默认已开）           影响效果
   ```
2. **归一化统计量已经写进 checkpoint**。推理时按**键名**查表，
   所以推理侧的 `observation.state` 维度、相机键名、`action` 维度必须和训练时**一模一样**。
   这是最常见的报错来源，而且报错信息通常指向归一化层，不会告诉你"是键名对不上"。
3. **loss 低 ≠ 成功率高**。模仿学习的 loss 会很快降到一个平台然后几乎不动。
   真正的指标是下一节的**闭环成功率**。本项目里 4/8 个动作维度是常量，
   这部分 loss 会瞬间降到 0，把总 loss 拉得很好看——**更要看闭环结果**。

画 loss 曲线：
```bash
python3 scripts/09-plot-loss.py logs/23-train-smolvla.log artifacts/train_loss.png
```

### B.6 闭环评测（这一步才是"验证"）

```bash
python3 scripts/06-eval-policy.py --config-name pick_and_place \
  +env.viewer.disable=true \
  +eval.checkpoint=$SIMRUN/artifacts/train_smolvla/checkpoints/last/pretrained_model \
  +eval.episodes=50
```

用 aao 的 `PolicyEvaluator`：它**不生成动作**，只负责把外部策略的动作施加到环境，
并按任务定义判定每个 stage 的成功/失败。所以拿到的不是一个 0/1，而是：

- 总成功率
- **卡在哪个 stage**（`pick_source` 还是 `place_source`）
- 失败原因（例如 `operator is not grasping the required object`）

**失败归因的三分法：**

| 现象 | 大概率是什么问题 |
| --- | --- |
| 夹爪根本没往物块方向去 | **视觉/语言没对齐**——换个语言指令看输出变不变，不变就说明语言分支没起作用 |
| 去对了但抓空、偏几毫米 | **数据量或精度不足**——加数据；检查腕部相机是否真的进了输入 |
| 抓起来了但放偏 | **长程误差累积**——action chunk 的开环段太长，缩短 `n_action_steps` 试试 |
| 只在某些随机初始位置失败 | **训练数据的初始位姿覆盖不够**——扩大 `randomization` 范围重采 |
| 训练分布内成功、换物块颜色就崩 | **过拟合**——小数据 VLA 的典型症状 |

> **报成功率必须同时报对照组。**
> 脚本化专家在同样的随机种子上跑 50 次的成功率是这个任务在这个仿真里的**上界**。
> VLA 只能逼近它，不可能超过。只报 VLA 的数字没有意义。
> 本次专家基线：`aao_demo --config-name pick_and_place` → **2/2**（batch_size=2）。

**本项目一个已知的天花板**：4/8 个动作维度是常量（见 B.4.9），
所以这个任务对 VLA 来说其实只是"学 x/y/z + 夹爪开合"。
成功率高不代表模型学会了姿态控制——**这一点必须说清楚，不要含糊过去。**

### B.7 本次跑出来的完整结果

**训练**（`logs/23-train-smolvla.log`，曲线 `artifacts/train_loss.png`）：

```
数据集 60 集 / 14954 帧    batch_size=24   steps=12000   lr peak 1e-4 cosine + 1000 warmup
450M 总参数 / 100M 可训练（视觉塔按 SmolVLA 默认冻结）
显存 6.3 GB / 48 GB       2.2 步/秒       全程约 90 分钟
loss: 1089.2 → 0.019
```

**闭环成功率**（每点 40 集，同一批随机种子 5001–5020，`artifacts/success_curve.png`）：

| | 成功率 | Wilson 95% CI | 成功时中位步数 |
| --- | --- | --- | --- |
| 脚本化专家（上界） | **40/40 = 100%** | [91.2%, 100%] | 249（min 245 / max 254） |
| SmolVLA @4000 步 | 35/40 = 87.5% | [74.0%, 94.5%] | 176 |
| SmolVLA @8000 步 | **40/40 = 100%** | [91.2%, 100%] | 185 |
| SmolVLA @12000 步 | 39/40 = 97.5% | [87.1%, 99.6%] | 177 |

**四个必须讲清楚的结论：**

**1. 4000 → 8000 步是真提升，8000 → 12000 的回落不是。**
前者置信区间几乎不重叠；后者只差 1 集，区间完全重叠。
> **n=40 根本分辨不出 2.5 个百分点。** 想说"12000 步过拟合了"，
> 先算一下样本量够不够——不够就只能说"没有观察到显著差异"。

**2. 置信区间必须用 Wilson，不能用正态近似。**
`p ± z·√(p(1-p)/n)` 在 p=1 时区间宽度是 **0**，等于宣称"100% 绝对可靠"，显然荒谬。
Wilson score 给出 `[91.2%, 100%]`——**40 次全对，真实成功率也可能只有 91%**。
这是报成功率时最容易出错、也最容易被追问的地方。

**3. VLA 成功时比专家还快（中位 185 步 vs 249 步）。**
专家按 YAML 里预设的 waypoint 一板一眼地走（`pre_move` → `eef` → `post_move`），路径保守；
VLA 从数据里学到的是一条更直接的路径。**但这不是优点，是风险**——
它抄的是近路，没有专家那套"先抬高再下降"的安全裕度。

**4. 失败形态是"卡死"而不是"做错"。**
所有失败集的 `failed_stage_counts` 都是空的，`steps` 都恰好等于 `max_updates=400`。
说明策略没有触发任何 stage 的失败判定，而是**在某个位姿附近停住不动直到超时**。
```
[6/40] seed=5003 success=[True, False] steps=400 failed_at=[]
```
这是模仿学习的典型失效：进入训练数据没覆盖的状态后，
模型输出的动作把自己锁在原地（预测"保持当前位姿"是训练集里最安全的平均答案）。

> 效果视频：`artifacts/vla_rollout_OK_seed5002.mp4`（成功）
> 与 `artifacts/vla_rollout_FAIL_seed7001.mp4`（卡死超时）。
> **两段并排看，就是最好的失败归因材料。**

### B.8 这个结果的天花板在哪（必须主动说明，不要含糊）

成功率 100% 听起来很好，但有三个前提必须讲清楚，否则一被追问就崩：

1. **4/8 个动作维度是常量**（见 B.4.9）。专家全程保持同一个末端姿态，
   所以这个任务对模型来说其实只是"学 x/y/z + 夹爪开合"，**根本没考姿态控制**。
2. **评测和训练是同一个仿真、同一套随机化范围**（物块 x/y ±3 cm）。
   这不是泛化测试，是**分布内测试**。换个物块颜色、换个光照、换张桌子，结果未知。
3. **仿真数据没有人类的纠错行为**。脚本化专家永远走同一条完美路径，
   数据里没有"走错了再修正"的样本，所以模型学不会从失败中恢复——
   这直接解释了为什么失败形态是"卡死"。

**诚实的说法**：
> "在与训练同分布的仿真环境里，SmolVLA 在 40 集评测中达到了与脚本化专家相同的成功率。
> 但这个任务的动作空间里有一半维度是常量，且评测未做分布外测试，
> 所以这个数字只能说明训练链路是通的，不能说明模型的泛化能力。"


### B.9 追查「4 维常量」的根因：一组对照实验

B.4.9 发现 8 维动作里有 4 维标准差是 0，当时的猜测是"采集时没打开姿态随机化"。
`pick_and_place.yaml` 里确实有一行被注释掉的：
```yaml
      # yaw: [-0.524, 0.524]   # ±30°
```
**但"打开它"并不是答案。** 下面是完整的追查过程。

#### 实验 1：直接打开 yaw 随机化

```bash
python 12-expert-baseline.py --config-name pick_and_place_yaw --episodes 20
```
> 日志：`logs/32-expert-yaw.log`

**结果：脚本化专家自己从 100% 掉到 40%（8/20）。**

而且失败形态和 VLA 完全不同——是**真的 stage 失败**，不是超时：
```
failed_stage_counts: {'pick_source': 8, 'place_source': 4}
seed=5001 success=[False, True] steps=241 failed=['pick_source']
```

#### 根因：位置跟随物块，姿态却是硬编码的

```yaml
pre_move:
  - position: [0.0, 0.0, 0.12]
    orientation: [0.0, 0.70710678, 0.0, 0.70710678]   # ← 写死的绝对四元数
    reference: object_world                            # ← 只有位置跟着物块
```

物块一转，夹爪还是按原角度扎下去，自然抓不住。

> **所以那行 `# yaw` 被注释掉不是作者偷懒，是脚本化专家结构上做不到。**
> 想靠"打开随机化"拿到有姿态方差的数据，得先让专家会转手腕。

#### 实验 2（失败）：猜 `relative rotation`

试着把 `orientation` 换成 `rotation: [0, 1.5708, 0]` + `relative: true`：

**结果 0/20，而且全是超时**（`failed_stage_counts` 为空）。
比不改还差——说明这个改法直接把目标位姿弄坏了，机械臂根本没动起来。

> **教训：改配置前先读语义，别靠猜。** 我这一步浪费了一轮实验。

#### 实验 3：读文档找到正确语义

`docs/task-configuration/stages_and_waypoints.md` 里写得很清楚：

```
- When `site` is set, its world pose replaces the `object` body's pose as
  the reference origin for `object_world` waypoints.
- For `reference: object`, the site's orientation is also used as the
  reference orientation.
```

**`object_world` 只继承位置，`object` 连朝向一起继承。** 这就是我要的。

```yaml
  - position: [0.0, 0.0, 0.12]
    rotation: [0.0, 1.5707963, 0.0]
    reference: object          # ← 不是 object_world
```

#### 这组实验的价值

1. **它把"我发现数据有问题"升级成了"我查清了为什么"。**
   前者只是观察，后者是诊断。
2. **它暴露了脚本化策略的本质局限**：所有适应性都必须由人显式写进 YAML。
   物块会转？那得有人想到并写出"姿态跟随物体朝向"这条规则。
3. **它正好指出 VLA 应该赢在哪。**
   VLA 从图像里学"该怎么转手腕"，不需要有人把规则写下来。
   > 但前提是**训练数据里得有转手腕的样本**——而这又回到了实验 1 的死结：
   > 专家不会转，就采不到；采不到，模型就学不会。
   > **这就是为什么真机项目要用人类遥操作而不是脚本化专家采数据。**
4. **它解释了本项目 100% 成功率的天花板**（B.8 第 1 条）到底是怎么来的。

> **一句话总结**：
> "我发现动作空间里有一半维度是常量，追下去发现根因不是配置没开，
> 而是脚本化专家的抓取姿态是硬编码的、不跟随物体朝向——
> 打开随机化后专家自己成功率从 100% 掉到 40%。
> 这件事让我理解了为什么真机数据必须靠人类遥操作采集：
> 脚本策略的适应性上限就是写规则的人的想象力。"


### B.10 std=0 不只是"白送 loss"，它会污染整个 loss 尺度

修好数据集之后训第二版，第一个 log 就露馅了：

```
v1（4 维 std=0）    step:100  loss:1089.237   grdn:745.354
v2（8 维都有方差）   step:100  loss:1.627      grdn:8.713
```

**同一个模型、同一套超参、同一个任务，起始 loss 差了 670 倍。** 为什么？

#### 追下去：LeRobot 的归一化怎么处理 std=0

`lerobot/processor/normalize_processor.py`：
```python
eps: float = 1e-8
...
denom = std + self.eps          # 避免除零
return (tensor - mean) / denom
```

**它不报错，只是加了个 `eps=1e-8`。** 于是 std=0 的维度实际是「除以 1e-8」。

#### 算一遍就明白了

旧数据集里 `qy` 恒为 0.7071067690849304，但统计出来的 mean 是 0.707107424736023
——**float32 的舍入残差**：

```
残差 = max - mean = -6.557e-07
归一化后 = -6.557e-07 / 1e-8 = -65.6        ← 本该是 0 的维度，变成了 ±65
进 MSE(平方) 的贡献 ≈ 4.3e+03
```

**8 个维度里有 4 个在贡献这个量级的噪声**，而真正有意义的 x/y/z/grip 归一化后是 O(1)。
所以 v1 的 loss 从头到尾被这四维的浮点噪声主导。

#### 三个后果（这才是重点）

1. **loss 数值完全没有可比性。** v1 的 1089 → 0.019 看着降了 5 万倍，
   但降的主要是那四维噪声，不是任务性能。**跨数据集比 loss 是没有意义的。**
2. **B.7 里那个"1000 步断崖"是假象。** 不是模型突然学会了任务，
   是它把那四维压到了残差以下。真实的任务学习进度得看闭环成功率。
3. **梯度被污染。** `grdn` 从 745 降到 8.7 也是同一个原因。
   训练早期的梯度方向有相当部分是在拟合浮点噪声——**这是实打实的算力浪费**。

#### 该怎么做

| 做法 | 评价 |
| --- | --- |
| **修数据源**（让退化维度真正有方差） | ✅ 本次采用，见 B.9 |
| 从特征里剔除退化维度 | ✅ 干净，但换任务要重来 |
| 把 `eps` 调大（如 1e-3） | ⚠️ 治标，噪声量级压下去了但维度还是废的 |
| 什么都不做 | ❌ loss 不可读，还白烧算力 |

> **一句话**：
> "我训第二版时发现起始 loss 从 1089 变成 1.6，追下去发现是归一化里 `std+1e-8` 的问题——
> 常量维度的 float32 舍入残差被放大了 65 倍，平方进 MSE 就是四千多。
> 也就是说我第一版看到的 loss 曲线，主要在描述浮点噪声，不是任务性能。
> 这件事之后我的习惯是：**看 loss 之前先看归一化统计量**。"


## 还能往下做什么

按性价比排序：

1. **安全兜底层**（最能体现工程思维）
   现在 VLA 输出直接下发，没有任何限位/速度/碰撞检查。
   加一层："VLA 出目标位姿 → MoveIt 校验 IK 可达 + 无碰撞 → 通过才执行"。
   这就把 Part A 和 Part B 真正接起来了，而不是两个独立的东西。

2. **Sim2Real 差距量化**
   同一个 checkpoint，在 MuJoCo 里和在真机上的成功率差多少、差在哪个 stage。
   没有真机就退而求其次：在仿真里加噪声（相机噪声、动作延迟、位姿扰动）看成功率怎么掉。

3. **数据量 - 成功率曲线**
   10 / 20 / 40 / 80 条数据分别训一次，画一条曲线。
   这条曲线能直接回答"还要标多少数据"，是工业界最关心的问题之一。

4. **rotation_6d 对比四元数**
   同样的数据，只改姿态表示，看成功率差多少。这是个小而漂亮的消融实验。

---

## 附：日常启动的最短命令链

```bash
# ---- MoveIt 2 仿真 ----
cd /home/tz/workspace/airbot/sim-rerun
./scripts/01-sim-demo.sh                    # 终端 A
. scripts/00-shell.sh                       # 终端 B，然后随便敲 ros2 命令

# ---- MuJoCo / VLA ----
cd ws/auto-atomic-operation
export PATH=/home/tz/workspace/airbot/sim-rerun/ws/venv-vla/bin:$PATH
export MUJOCO_GL=egl
aao_demo --config-name pick_and_place +env.viewer.disable=true
```

## 附：安全提醒

原资料 `26-07-19求之科技实训/` 里有三处明文凭据（API key、机场节点 uuid/password、云服务器 SSH 地址），
原始资料目录含明文凭据，未纳入本仓库。
